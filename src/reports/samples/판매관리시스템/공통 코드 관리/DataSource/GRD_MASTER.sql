SELECT GROUP_CD
	 , MAX(CASE WHEN CODE='$' THEN CODE_NAME END) AS CODE_NAME
	 , COUNT(CODE)-1							  AS CODE
	 , MAX(CODE_DESC)							  AS CODE_DESC
	 , MAX(SORT_ORDER)							  AS SORT_ORDER
  FROM SM_COMMON_CODE
 GROUP BY GROUP_CD
 ORDER BY GROUP_CD